import 'package:flutter/material.dart';

class ChildListTile extends StatelessWidget {
  const ChildListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
